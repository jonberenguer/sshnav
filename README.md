> [!IMPORTANT]
> **The source code for this repository was generated by claude.ai Sonnet4.6**
> This is strictly to review code generated by AI providers, which may follow with modifications in the future.

> [!WARNING]
> This project is provided **as-is** without warranty of any kind.
> Review the code before use in any environment you care about.
> Use at your own risk.

---

# sshnav

A terminal UI for managing SSHFS mounts and SSH ProxyJump tunnels — built with [Bubbletea](https://github.com/charmbracelet/bubbletea).

---

## Requirements

| Tool | Purpose |
|------|---------|
| Go ≥ 1.22 | build |
| `sshfs` | mounting remote filesystems |
| `fusermount` | unmounting (part of `fuse` / `fuse3`) |
| `ssh` | ProxyJump tunnels |

```
# Debian/Ubuntu
sudo apt install sshfs fuse3

# Arch
sudo pacman -S sshfs

# Fedora
sudo dnf install fuse-sshfs
```

---

## Build & Install

```bash
# Build only (binary in current directory)
bash build.sh

# Build + install to /usr/local/bin
INSTALL_DIR=/usr/local/bin bash build.sh

# Custom install path
INSTALL_DIR=~/.local/bin bash build.sh
```

> Builds run inside Docker (`golang:1.22-alpine` → `scratch`). No local Go toolchain required.

---

## Usage

```
sshnav [--profiles-only]        Launch the TUI
sshnav export-ssh-config        Convert profiles.yaml → ~/.ssh/config format (stdout)
sshnav import-ssh-config        Convert ~/.ssh/config → profiles.yaml format (stdout)
```

### Flags

| Flag | Default | Description |
|------|---------|-------------|
| `--profiles-only` | false | Skip `~/.ssh/config` — only load app-managed profiles |

---

## Config files

| File | Purpose |
|------|---------|
| `~/.config/sshnav/profiles.yaml` | App-managed profiles (read/write) |
| `~/.ssh/config` | Imported as read-only profiles |

### profiles.yaml example

```yaml
- name: my-nas
  host: 192.168.1.50
  user: admin
  port: 22
  identity_file: ~/.ssh/id_ed25519
  remote_path: /mnt/data
  mount_point: /mnt/nas
  sshfs_opts: reconnect,ServerAliveInterval=15

- name: prod-bastion
  host: internal.prod.example.com
  user: deploy
  proxy_jump: bastion.example.com
  local_forwards:
    - "8080:localhost:80"
  remote_forwards:
    - "9090:localhost:9090"
```

All fields except `name` and `host` are optional.

---

## Screens & Keybindings

### Dashboard (default)

| Key | Action |
|-----|--------|
| `↑↓` | Navigate profiles |
| `Enter` | Open action submenu for selected profile |
| `p` | Open Profile List / manager |
| `n` | New profile |
| `e` | Edit selected profile (app-managed only) |
| `/` | Filter profiles |
| `Ctrl+C` | Quit (closes all active tunnels) |

Live status dots (`●`/`○`) are polled every 5 seconds. On wide terminals (≥ 90 columns) a right-hand detail panel shows proxy jump chain, SSHFS paths, and port forwards for the selected profile. In filter mode the panel is hidden and the list expands to full width.

#### Action Submenu

Pressing `Enter` on a profile opens a submenu with context-aware actions:

| Key | Action |
|-----|--------|
| `s` | Open interactive SSH session (TUI suspends, full PTY) |
| `m` | Open SSHFS panel (only shown when mount is configured) |
| `t` | Open Proxy/Tunnel panel |
| `↑` / `k` | Move cursor up |
| `↓` / `j` | Move cursor down |
| `Enter` | Execute highlighted action |
| `Esc` / `q` | Close submenu |

Both letter shortcuts and cursor + Enter work interchangeably.

### Profile List

| Key | Action |
|-----|--------|
| `↑↓` | Navigate |
| `Enter` / `e` | Edit selected (app-managed only) |
| `c` | Duplicate selected profile as a new copy |
| `n` | New profile |
| `d` / `Delete` | Delete (app-managed only, confirms with y/N) |
| `Esc` | Back to Dashboard |

On wide terminals (≥ 90 columns) a right-hand panel shows the full details of the selected profile: source badge, SSH address, identity file, proxy jump, SSHFS paths and options, and all port forwards.

### Profile Edit / Create

| Key | Action |
|-----|--------|
| `Tab` / `↓` | Next field |
| `Shift+Tab` / `↑` | Previous field |
| `Ctrl+S` | Save |
| `Enter` | Advance field (save on last) |
| `Esc` | Cancel |

Fields are split into sections: **SSH**, **SSHFS**, and **Proxy**.

### SSHFS Panel

| Key | Action |
|-----|--------|
| `m` | Mount / Unmount |
| `e` | Edit profile |
| `Esc` | Back to Dashboard |

Mount state is read from `/proc/mounts` (looks for `fuse.sshfs` entries).

### Proxy Panel

| Key | Action |
|-----|--------|
| `t` | Open / Close tunnel |
| `s` | Open interactive SSH session |
| `e` | Edit profile |
| `Esc` | Back to Dashboard |

Each port forward is shown with a live status indicator:
- **Local forwards**: `●` when something is listening on the local port, `○` when not.
- **Remote forwards**: `●` when the tunnel is active (bind success implied by `ExitOnForwardFailure=yes`), `○` when not.

#### Tunnel behaviour

The tunnel runs `ssh -N` with `ExitOnForwardFailure=yes`, `ConnectTimeout=15`, `BatchMode=yes`, and `ServerAliveInterval=30`. A 2-second grace period confirms the process is still alive before reporting "connected" — this prevents false positive status when SSH fails immediately after start. The process is owned by sshnav and killed on close or quit.

Local port forwards are pinned to `127.0.0.1` (e.g. `8080:host:80` → `127.0.0.1:8080:host:80`) to ensure both IPv4 and IPv6 listeners are created on dual-stack hosts.

---

## Architecture

```
sshnav/
├── main.go                 # Bubbletea entrypoint
├── config/
│   └── config.go           # Profile types, YAML load/save, ~/.ssh/config parser
├── sshutil/
│   └── sshutil.go          # sshfs mount/unmount, SSH tunnel, /proc/mounts polling
└── tui/
    ├── styles.go           # Lipgloss styles & shared helpers
    ├── app.go              # Root model, screen router, shared state
    ├── dashboard.go        # Profile list with live status indicators
    ├── profilelist.go      # CRUD profile manager
    ├── profileedit.go      # Text-input form for create/edit
    ├── sshfs.go            # Mount/unmount panel
    └── proxy.go            # ProxyJump tunnel panel
```

---

## Notes

- `~/.ssh/config` hosts are **read-only** — they appear in the dashboard and panels but cannot be edited or deleted from within sshnav. Edit them in your normal SSH config workflow.
- SSHFS mounts survive sshnav exiting (they are kernel FUSE mounts). SSH tunnels do **not** — they are subprocess-owned and are killed on quit.
- `sshfs_opts` is passed as comma-separated `-o key=value` pairs. Refer to `man sshfs` for available options.
- `LocalForward` / `RemoteForward` lines in `~/.ssh/config` use space-separated format (`9090 localhost:80`); sshnav normalises these to colon-separated format (`9090:localhost:80`) automatically at parse time.
- Interactive SSH sessions (`s` key) suspend the TUI and hand the terminal directly to `ssh`. The TUI resumes cleanly after the session ends.
